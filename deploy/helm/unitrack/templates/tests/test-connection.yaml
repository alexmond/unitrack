apiVersion: v1
kind: Pod
metadata:
  name: {{ include "unitrack.fullname" . }}-test-connection
  labels:
    {{- include "unitrack.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: health
      image: curlimages/curl:8.11.1
      command:
        - sh
        - -c
        # Actuator is on the internal management port (not the Service), so probe a public app
        # endpoint over the Service instead. /login is permitAll and returns 200 in any auth mode.
        - >-
          curl -sf --retry 30 --retry-delay 2 --retry-connrefused
          http://{{ include "unitrack.fullname" . }}:{{ .Values.service.port }}/login
