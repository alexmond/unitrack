# Brainstorm Panel — log

Evidence from past panels in this repo. Bias future proposals with it; it's not law.

## 2026-06-10 — UI critique vs competitor screenshots (improve existing artifact)
- Proposed: Visual/UI Designer (director), Frontend/Design-system Eng, Competitor/Market Analyst, UX/Information Architect, QA/Tester, Restraint Skeptic. User edits: **none** ("go" as-is) — confirms the default UI-target roster (design trio + Analyst + QA + Restraint) is well-calibrated here; stop re-deriving it.
- Style: swarm → director-led, **1 round** (very strong convergence). Vote: **wow = 4 yes-subtle / 2 no**, but the two "no" = "functional-not-decorative", so effective consensus = **subtle, functional-first wow; nobody wanted bold/animated flash**.
- UNANIMOUS #1 (all 6): the **front door under-delivers on glanceable health** — index project cards show only "N runs"; can't tell which project is red without clicking. Fix = health cards (status pill + pass%/coverage% + last-run time + colored left border, failing-first) — CSS-only, Skeptic-approved.
- Strong multi-role: pass-rate **donut + quality-gate badge hero** on project Overview + Run header (Chart.js, where it already loads); **tint FAILED rows**; **KPI tiles w/ threshold colors + trend deltas**; **Chart.js theme-toggle fix** (real debt — light charts inherit dark gridlines; do FIRST); **de-emoji subnav → mono icons**; trend y-axis 50–100 + shorter; contrast bumps (muted token, dark coverage-bar empty track, orange-on-dark WCAG). QA caught a real **data-trust bug**: duplicate X-axis commit label.
- Director's key call on the panel's one real split — **donut-on-index (Designer/Analyst) vs text-only-index (Skeptic)**: index stays **CSS-only health cards (no Chart.js)**; reserve the donut hero for Overview/Run where Chart.js already lives. Threads cheap-win + wow.
- Recurring (now 5×+): **Restraint Skeptic wins scope** — flagged the real over-reach isn't the "flat" look, it's the **9-tab sprawl** (Failure clusters/Triage = competitor-envy); don't add a 10th tab. Explicit SKIPs (Skeptic+Analyst agreed): drag-drop widget builder / saved dashboards / "+Widget" canvas (RP+TestOps — configurability w/o curation looks broken), SonarQube A–E grade, ML/auto-quarantine, treemap, manual test-case mgmt.
- Positioning win to lead with: **unified tests+coverage+perf+flaky+load in one tool** — no competitor in the set does all four; the Run-detail page already beats per-tool rivals. Fix is polish on Index/Overview, not features.
- Note (durable): when critiquing UI here, capture BOTH our screens (headless-Chrome the demo, dark+light) AND the competitor `doc/images/` — visual side-by-side, not abstract. Gotchas hit: force theme via `localStorage('theme')`+reload (emulation alone loses to a persisted value in the CDP profile); size the viewport to content height before capture or **Chart.js canvases blank out** under captureBeyondViewport.
- **Tier-1 acceptance review (2026-06-10):** re-convened Designer/QA/Restraint/Frontend on the merged Tier 1 → **all SIGN-OFF-WITH-NITS**. Confirmed delivered: health cards, failed-row tint, trend-axis dedup (`a3c0de3 ·2`), coverage threshold colours, contrast bumps — tasteful, CSS-only, **no scope creep** (Skeptic confirmed discipline held). Remaining nits, all small: (1) **light-mode FLAG-cell text** (`default`) still washed on white — a residual contrast miss; (2) the **index empty void** (Skeptic's original core complaint) still unaddressed — cheapest fix is footer `margin-top:auto` + `min-height:100vh` flex, NOT decoration; (3) **failing-first sort unverifiable** — all demo latest-runs PASS, so the red rail/sort never shows (seed one failing project to demo it). Known Tier-2 prereq still open: Chart.js theme-toggle re-render (#166) — gridlines baked at construct time. Lesson: re-convening the same panel for an acceptance pass works well (1 round, focused on the implemented subset; skip roles whose items weren't built yet).
- **Tier-2 acceptance review (2026-06-10):** re-convened Designer/Frontend/QA/Restraint on merged Tier 2 (donut #167, theme-toggle re-render #166, trend area-fill+y-axis #168, de-emoji icons #169) → **all SIGN-OFF-WITH-NITS**. Confirmed: donut hero is the right "subtle wow" + survives the failing-run case (green/red split), theme re-tint fixed (light charts no longer keep dark gridlines), icons render as real glyphs. **Strongest nit (2 of 4 ✗): the `suggestedMin:50` y-axis was MISLEADING** — a 75% dip read as a collapse on a metrics tool. Reverted to a 0 floor (kept area fills). Other nits fixed: stray emoji on the "Push results from CI" expander (3 reviewers), donut-centre red-tint on fail, Coverage icon umbrella→shield-check. **Tension overruled:** Restraint wanted to cut the bootstrap-icons WebJar (font weight) for inline SVG — but the user's standing **"WebJars when possible"** preference outranks it (keep the WebJar). Lesson: the Restraint Skeptic's dependency-scope instinct must yield to an explicit user preference; capture such preferences as memory so the panel's default can be overridden knowingly.

## 2026-06-09 — #21 Configurable widgets + real-time ingest (design → epic+tickets)
- Proposed: Product/UX Lead (director), Real-time Backend Architect, Frontend/Dashboard Engineer, Restraint/YAGNI Skeptic, QA/Reliability Tester. User added: **Competition/Market Specialist** ("add competition specialist and go"). User removed: none.
  - Gate-edit signal: user adds a **competitor-research seat** on differentiator/roadmap targets (cf. the uploader run). The Competition specialist pulled real weight — cited `doc/competitor-analysis.md` to confirm SSE-over-broker and to *kill* the drag-drop widget grid as enterprise bloat. **Seat a Competition/Market analyst by default on differentiator-framed features here.**
- Style: swarm → director-led, converged in **1 round** (strong: 5/6 aligned on transport AND sequencing). Vote: transport **SSE 5 / polling 1**; sequencing **realtime-first 4 / widgets-first 1 / cut-widgets 1**.
- Outcome: SSE (`SseEmitter`, zero new deps) + `@TransactionalEventListener(AFTER_COMMIT)` decoupled broadcast (never fail ingest) + emitter registry + `/api/v1/events` + heartbeat; live index cards + project recent-runs/trend, page-scoped, no toasts. Widgets half **deferred + shrunk** to per-user show/hide via boolean columns on `User` (notification-prefs pattern) — NO drag-drop/catalog. Epic #21; tickets #145–#150.
- Recurring (now 4×+): **Restraint Skeptic wins dependency-scope** — but here SSE already needs no new dep, so the Skeptic's real win was *shrinking the widgets half to near-zero* and *deferring it*, not cutting transport. The decoupling-from-ingest (QA's non-negotiable) + the AFTER_COMMIT contract were unanimous across backend/QA/product.
- Note (domain): the **upload/CI client needs zero changes** for real-time — it just POSTs `/api/v1/ingest`; real-time is pure server→browser push to viewers. `@EnableScheduling` already on (heartbeat is free). No SSE/WS stack exists today → greenfield, dependency-free. nginx footgun to document: `proxy_buffering off` + `X-Accel-Buffering: no` header or SSE silently never flushes.

## 2026-06-09 — Bootstrap app-wide adoption, re-eval (choosing between options)
- Proposed: UI/Frontend Architect (director), Design-system Guardian, Build/WebJars Engineer, Migration Strategist, Accessibility Advocate, Restraint Skeptic, QA (pre-seated). User added/removed: none.
- Style: swarm → director-led, 1 round (strong substantive convergence). Vote: **0 ADOPT-APPWIDE / 5 ADOPT-PARTIAL / 2 STAY-CUSTOM** (Architect + Skeptic).
- UNANIMOUS substance regardless of label: (1) **no big-bang 16-template rewrite**; (2) Bootstrap's real win is JS-wired components (modal/dropdown focus-trap) — UniTrack has **zero** today; (3) first modal → native `<dialog>` (0 deps) before Bootstrap JS; (4) IF adopting, **bridge-first**: `--bs-*: var(--app-*)` (app tokens master), toggle also sets `data-bs-theme`, ~20-30-line bridge lands before any component; keep theme tokens, Chart.js var-reads, status badges, `.gate/.bar/details.failure/.snippet/pill-subnav`.
- Only real split = **timing** (start incremental now vs wait for a trigger). Trigger to justify full adoption: ≥2 focus-managed JS components, OR a responsive/mobile need, OR ≥2 active contributors. None true today.
- Recurring (4th run): Restraint Skeptic wins dependency-scope — BUT this run is a genuine 5-2 split; the disagreement is "start now vs wait," not "how." If the user keeps re-invoking Bootstrap, that itself may be the trigger (maintainer preference) — honor it via the bridge-first incremental path, not a big-bang.
- Note (durable tech): `org.webjars:bootstrap:5.3.8` (Boot BOM does NOT manage it — pin version); Spring Boot 4 serves `/webjars/**` by default (add-mappings on); assets ship in-jar (offline-good for self-hosted); CSS-only ~30KB (skip `bootstrap.bundle.min.js`/Popper until a modal/dropdown lands); skip `webjars-locator` (hardcode versioned path, single-source via a `${bootstrap.version}` property); keep emoji (no Bootstrap Icons). Bridge needs `-rgb` token forms set literally per-theme. Chart.js does NOT re-render on theme toggle today (pre-existing, not a migration bug).

## 2026-06-09 — Client-side result-pushing / CI uploader (design → epic+tickets)
- Proposed: DevEx Director, Competitive Analyst (web research), Release/Packaging Engineer, SDET, Polyglot Ecosystem Advocate, Security/Supply-chain, YAGNI Skeptic. User added: **QA** ("add qa person"). User removed: none.
  - Signal (3rd consecutive design target the user adds a QA/consumer seat): **graduate — seat QA + SDET by default on design/product targets here.** QA(upload-reliability) and SDET(pipeline-author) were genuinely distinct and both pulled weight (QA: fail-loud-on-0-match + a real exit-code contract; SDET: PR head-SHA vs merge-SHA, `if: always()`, shard runKey).
- Style: swarm → director-led, converged in **1 round** (7/8 aligned). Director-led fits a single-vision DevEx target.
- Outcome: auto-detect CI metadata in the bash script (kill hand-wired `$GITHUB_*`) + reliability hardening + a composite Marketplace Action wrapping the SAME script + per-ecosystem recipes; Docker fast-follow; ingest-scoped token (server). Rejected with explicit triggers: Go binary, Maven/Gradle plugins, npm/pip pkgs, `curl|bash` installer, OIDC.
- **User gate-edits (overruled the panel's defer + reshaped the architecture):** the panel deferred a compiled binary and Maven/Gradle plugins and proposed bash as the core. The user steered: (1) a **Java JAR uploader** is the "most versatile in the long run … can lead to Gradle/Maven plugin," and (2) **"Docker / action can run [the] Java agent."** → Converged on **the JAR (`unitrack-cli`) as the single upload engine**, with Docker (JRE+JAR), a Docker-based GitHub Action, and the Maven/Gradle plugins all just *running/wrapping that one JAR*; bash drops to a minimal dependency-free fallback. Key lesson: the YAGNI objection to a binary was *"a second toolchain,"* which **does not apply to a JAR** (same JVM build the repo already has) — and a single JVM engine behind every delivery vehicle is less duplication than parallel bash+JAR. For THIS repo, default to a JAR-based core for any "client tool" target.
- Recurring (now 3×): **the YAGNI/Restraint skeptic wins the dependency-scope call** — minimal set over a sprawl; always capture "build later, only if X" exit ramps. (Caveat from this run: scope-skepticism must distinguish *new-toolchain* cost from *same-toolchain* cost.)
- Note (domain): competitor pattern = thin Action wrapping a binary + auto-detected metadata (Codecov/Datadog/Coveralls/Sonar); UniTrack's gap was the Action + auto-detect, not transport. CI-docs footguns to always flag: .NET TRX ≠ JUnit (needs `JunitXml.TestLogger`), JaCoCo XML is opt-in, `jest-junit` isn't default, Go needs `gotestsum` + `gocover-cobertura`. The shipped example workflow inlined raw `curl` (divergent from `scripts/unitrack-upload.sh`) — make the script the single source of truth.

## 2026-06-09 — Project sub-nav redesign (improve existing UI component)
- Proposed: Visual/UI Designer (director), Frontend/Design-system Guardian, Build/Platform Engineer (WebJars), Accessibility advocate, Information Architect, Restraint Skeptic. User added: **QA/Tester** ("they are the main user"). User removed: none. User constraint: "consider using bootstrap using webjars."
  - Signal (confirms the prior two runs): the user adds **QA/Tester** to UI targets too, not just product/roadmap — seat it by default everywhere. QA surfaced the single highest-value finding: the nav had no active state AND lived only on project.html (sibling sub-pages had no nav at all) → extract a shared fragment used across all pages.
- Style: swarm → director-led, converged in **1 round** (unusually strong consensus — 6/7 aligned on both decision and design). Director-led fit: a nav wants one coherent visual owner.
- **Framework decision**: user asked to weigh **Bootstrap via WebJars**; panel voted 6 effective STAY-CUSTOM / 0 genuine adopt (IA abstained as stack-agnostic; QA's "ADOPT" was actually "adopt a shared fragment"). Reason: ~30KB + a SECOND theme system (`data-bs-theme` vs the app's `data-theme`) + a SECOND token set (`--bs-*`) duplicating already-hand-rolled `.card/.badge/.table`, for one nav. WebJars mechanics are fine (ships in-jar — good for self-hosted); revisit only as a deliberate app-wide migration IF a forms/modals/grids wave arrives. Don't smuggle a framework in under a cosmetic ticket.
- Recurring: the **Restraint Skeptic keeps winning** the dependency-scope argument (consistent with YAGNI vetoing RBAC/MCP earlier). The cheap, token-reusing custom path is the house style.
- Note (repo UI conventions for future targets): the design system is ONE hand-rolled `app.css` with CSS-variable dark/light theming via `data-theme` on `<html>`. New components should consume existing tokens (`--panel/--panel-2/--border/--radius/--muted/--blue`), add an active state via a controller-passed key + `th:classappend` + `aria-current`, and live in `fragments/common.html` as a parameterized fragment reused across sibling pages. Verify visual changes with the self-skipping `@Tag("ui")` Selenium screenshot test **SubnavScreenshotUiTest** (kept on purpose for future UI work — screenshots land in `target/screenshots/`).

## 2026-06-07 — Next feature to build (prioritization / open ideation)
- Proposed roster: Product Strategist (director), Competitive Analyst, Spring Boot Architect, DevEx/Adoption Advocate, YAGNI Skeptic. User added: **Tester (QA)** and **Automation Tester (SDET)**. User removed: none.
  - Signal: the two consumer/practitioner roles the user added (Tester, SDET) carried real weight — SDET surfaced the eventual #1 (machine-readable gate); Tester surfaced the #3 (blame-link). For a *product/roadmap* target in this repo, **seat Tester + SDET by default**.
- Style: **swarm → director-led**, 3-round cap (converged in 2). Fit well: swarm gave 7 independent rankings, director reconciled by value×differentiation÷effort. Keep for prioritization targets.
- Outcome top-3: 1) **Machine-readable quality-gate result** (XS — GateController/QualityGateResult already exist; gap = CI-consumable exit-code contract + by-commit lookup + wrapper script) 2) **Unit-test performance** (slowest-tests + duration trends; S, read-side over already-ingested durationMs; Round-1 Condorcet winner) 3) **First-failing-commit blame link** (S if scoped to "since last green on same branch", SHAs only).
- Recurring critiques to expect here:
  - **Effort re-grading.** R1 estimates were optimistic; the Architect's R2 pass moved PR-comment M→L and blame-link S→M. **Always run an Architect effort-validation pass before locking estimates.**
  - **Adoption-vs-internal-value split.** DevEx/SDET push external-pull features (gate contract, PR comment); Product/Competitive/Tester push internal value (perf, triage). With ~0 external users, the cheap *adoption unlock* (machine-readable gate) won by being XS, not by winning the philosophical argument.
  - **YAGNI consistently vetoes** RBAC (half-a-feature for a solo maintainer) and MCP (#54, speculative). Don't lead with those.
- Repo quirks worth respecting: per-test **durationMs already ingested** (perf features are pure read-side); QualityGateService baseline = latest prior run on base-branch + same flag; UniTrack stores commit **SHAs only** (no git diff/history) — anything "blame"/"first-failing" must be scoped to stored runs, not git.

## 2026-06-08 — Perf-test ingestion design (design / greenfield → epic+tickets)
- Proposed roster: Performance Engineer, Market Analyst (web research), Spring Boot Architect (director), Charts Advocate, SDET, YAGNI Skeptic. User added/removed: none (approved as-is).
  - Signal: the perf-domain version of "Tester" (Performance Engineer) + SDET were both pivotal — SDET drove the per-label "store now" call and the 422 status-code semantics; Perf Engineer set the headline metric (p95 + error rate). Confirms the seat-Tester+SDET default for product/design targets.
- Style: **swarm → director-led**, 3-round cap (converged in 2). Worked well for a design target: swarm gave 6 lenses incl. live market research; director synthesized into a data model + ordered ticket plan.
- Outcome: data model (perf_run + perf_transaction), JMeter JTL CSV first (k6 fast-follow, Gatling deferred), extend /api/v1/ingest with a `perf` param + PerfResultParser abstraction (copy CoverageParser), regression reuses baseline pattern (p95 +15% / tput −10% / err >1%), project perf trends UI. MVP first PR = data model + JMeter parser + ingest wiring.
- Recurring (confirmed again): **Architect effort-validation pass is essential** — it re-graded the JMeter JTL parser M→L (percentile computation from per-sample data is an aggregation pipeline, not a parse). Always run it.
- Note (domain): JMeter JTL is CSV per-sample (compute percentiles server-side); k6 JSON summary is pre-aggregated (trivial); Gatling simulation.log is brittle/undocumented — defer. Run-vs-run baseline comparison (BlazeMeter's signature) is table-stakes; reuse UniTrack's per-commit baseline. Differentiation: self-hosted + free + perf regression unified with tests+coverage.

## 2026-06-27 — Health-over-time / catch-slow-degradation (design brainstorm)
- Proposed: Product/UX Strategist (director), Reliability/SRE & Alerting Eng, Detection/Metrics Skeptic, Spring Boot Architect (+platform/effort mandate), Dashboard/Chart UX Designer, Competitive Analyst, Restraint Skeptic. User edits: **none** ("go" as-is).
  - Deviation that worked: seated **SRE + Detection/Metrics Skeptic** in place of the usual QA+SDET because the target was *detection rigor*, not test authoring. Both pulled decisive weight (SRE owned silence-detection + alert-noise lifecycle; Detection Skeptic killed the scoring formula). For *detection/alerting/metrics-soundness* targets here, swap QA+SDET → SRE + a Metrics/Detection skeptic.
- Style: swarm → director-led, **1 round** (strong convergence; Architect folded the effort-validation pass into round 1 again — keep doing this).
- **Decisive call — KILL `depth × age × exposure` raw multiplication** (Detection Skeptic, uncontested): unit-salad (a 200-build CI matrix outscores a dead project; any near-zero axis annihilates the product). v1 sort key = the **dumb `days_red desc`** (self-explanatory, ungameable). The richer score survives only as a *deferred, additive, normalized weighted-sum* (depth vs the project's own **trailing-median**, never last-green), added ONLY if a flat sort produces a demonstrably-wrong top-5. Onset always via **N-of-M hysteresis** so flaky churn can't fake a streak.
- Architect feasibility (grounded): Phase-1 "broken-since" (last_green_at, broken_since, days_red, runs_red) is **derivable from existing per-run rows via ONE set-based windowed query** (ROW_NUMBER/LAG partitioned by project+branch+flag, 90d-capped) — **no new columns, no ingest-path change, no N+1** (must not regress #284/#285). Chart data already in `trendRuns()`. Cadence-silence is the ONLY piece needing a scheduled sweep — pattern already proven by `web/notify/TokenExpiryReminder` (@Scheduled). Async-ingest epic #E6 is NOT a blocker (all read-side).
- Restraint wins again (7×+): MVP = board "broken-since" column + `days_red` sort, nothing else. **Defer** the true-time Chart.js axis (new date-adapter dep) → date-labelled categorical + a "no runs · Nd" gap marker until cadence is demonstrably misread; **defer** alerting (Phase 3, its own correctness surface) until the board column is in daily use and things are still missed; hard-cut ML anomaly detection + any TSDB.
- Designer: time-aware chart = dots colored by pass/fail (burst reads as red smear, not "busy"), no-run gaps as a labelled hatched band, onset/recovery vertical rules with a red-wash interval whose width = rot duration, "By run ↔ Over time" toggle (Over-time default), **hard 0–100 rate floor** (the misleading-suggestedMin trap from 2026-06-10 again). Board: regressed-only stat line; healthy cards gain nothing.
- Competitive: "regressed-since" + "reporting went silent" are genuine white space (rivals gate PRs vs base or chase flaky; none treat *absence of uploads* as signal). Lead README with it; avoid ML-anomaly envy.
- Delivered as a 3-phase plan (1: board broken-since + sort; 2: time/cadence chart; 3: rate+silence alerting). No code yet — awaiting user pick of phase/scope.

## 2026-06-15 — New roadmap epics (open ideation / prioritization)
- Proposed: Product Strategist (director), Competitive Analyst, Spring Boot Architect (+platform mandate), Tester/QA, SDET, Restraint Skeptic. User added: **UI/UX Designer** + an explicit instruction that **a stack/platform-foundation epic is a valid output when the stack blocks multiple features** (gave the Architect the platform mandate). User removed: none.
  - Gate-edit signal: on roadmap/ideation targets the user now also wants a **Designer seat** (experience-led epics: share links, badges, export, run-diff) — consider seating Designer by default alongside the graduated Analyst+QA+SDET. And "stack-gap-as-epic" is a standing ask — keep the Architect's platform mandate on roadmap targets.
- Style: swarm → director-led, **1 round** (very strong convergence; Architect did the effort-validation pass inline — keep folding it into round 1 for ideation).
- Cross-role consensus (the durable signal): **Test Ownership & Routing appeared in 5 of 7 roles** (#1 by consensus); **Actionable/write MCP copilot** (3 roles) is the clearest differentiation moat; **Alerting beyond email — Slack/webhook + noise-muting** (3 roles); **History search + run-comparison** (3 roles, keep on indexed Postgres columns, NOT a search engine).
- Architect platform finding (the user's stack-gap ask, grounded in code): ingest is one synchronous `@Transactional` method (parse+persist+gate+regression+GH-status+email on the CI thread); 100k inline blob columns; email-only notify; no FTS/object store; read-only 7-tool MCP. → **async ingest pipeline** is the chokepoint epic every alerting/search/routing/SSE feature piles onto.
- Skeptic wins again (6×+): killed ML-TIA, full-text-search infra, SPA rewrite, mobile app, plugin marketplace, multi-tenant SaaS, Sonar grades — each with a build-later trigger; flagged the brainstorm's biggest risk = competitor-envy bloat + a second toolchain/datastore. His 3 lean defends (retention/pruning, ingest hardening, backup/export) folded in as a foundation-hygiene cluster.
- Director calls: TIA = big-bet honorable-mention only (deterministic coverage-based + audit-first, after a per-test-coverage-granularity spike; never ML). Merge-gating folded into backlog #178 (don't double-count). Artifact/object-store deferred behind async ingest.
- Final shortlist: E1 actionable-MCP copilot, E2 alerting+eventing, E3 ownership+routing, E4 history-search+run-compare, E5 shareable reporting (public links+badges+export), E6 async-ingest platform.

## 2026-06-29 — Unify the 4 project analytics pages (improve existing UI; consistency)
- Proposed the calibrated UI roster (Designer-director, Frontend/Design-system, UX/IA, Competitive Analyst, QA, Restraint Skeptic). User edits: **none** ("Go") — confirms again the design-trio + Analyst + QA + Restraint default for UI targets; stop re-deriving.
- Style: swarm → director-led, **1 round** (strong convergence). Screenshots already existed for all 4 targets (`presentation/02,03,07,08` + maintainer pastes) — no capture needed; competitor images via web (Codecov/Datadog/Allure).
- **The one real split — KPI tiles + "single primary chart" on ALL pages (Designer/IA/Analyst) vs Restraint+QA.** Director calls: (1) **tiles go on all 4 but MUST carry Δ-vs-previous** — that's the glance the chart doesn't give, so they earn the slot and aren't "restating the chart" (Skeptic's valid objection answered by the delta, not by cutting tiles); shipped as an OPT-IN `kpiTiles` fragment. (2) **Keep Load tests' 3 charts — REJECT the metric-switcher** (Skeptic+QA win: a switcher hides the simultaneous 3-signal view that is the page's whole point); reconcile with the skeleton by making latency the primary chart + demoting throughput/error to a secondary two-up row. Both carve-outs are Restraint protecting real value — consistent with the 7×+ "Skeptic wins scope" pattern.
- Unanimous, acted on: one canonical band order (header → filter pills → provenance line → KPI tiles → primary trend w/ time/run toggle + clickable points → detail table(s) → `.empty`); **one filter idiom** (`.subnav.module-filter` pills w/ "All" + `aria-current`, present even at 1 option; convert Overview branch `<select>` → pills); **clickable trend points everywhere** (Coverage+Load charts are dead on click today — wire the shared `openCompare`); **consistent `.empty`** (add the missing Load perf-runs empty block; kill Timing's bare `<p>`); **statusPill everywhere** (drop raw `.badge` status); **naming fix** (Test-timing page H1/title/crumb say "performance" → rename; keep "Load tests"); detail tables: leading mono identifier = blue link to the row's detail.
- Frontend extraction (cheap, Skeptic-blessed): `pageHeader`, `filterPills(...)`, `kpiTiles(...)` fragments + a shared trend JS helper (`__chartColors()` preamble + `openCompare` + toggle). Skeptic cap: extract the color preamble + reuse `__chart`, do NOT abstract the chart configs.
- Keep deliberately DIFFERENT (unify the frame, not the body): Overview donut hero + the `healthOverlay` onset/no-runs plugin (Overview-only), Load's `perfStep` change-point banner (#379), Coverage per-row `.bar` gauges + package/file tables, Load per-label breakdown.
- Explicit CUTS (Skeptic+Analyst, build-later-on-demand): widget canvas / drag-resize tiles, saved per-user layouts, the Load metric-switcher, coverage treemap/sunburst, Sonar A–E grades. Anti-pattern named: Allure's freeform masonry — borrow card chrome, not the freeform grid.
- Delivered a 3-phase spec (1: shared frame — header/filter/empty/provenance/clickable-charts/statusPill/rename; 2: delta-bearing KPI tiles; 3: Load chart reflow). No code yet — awaiting maintainer's phase pick.

## 2026-06-29 — Project-page taxonomy follow-up (Overview/tests/coverage overlap; choose-between-options)
- Re-convened a focused 5-seat subset (UX/IA director, Product, Competitive, QA, Restraint) on the IA question the maintainer surfaced: "Overview conflates tests+coverage and there's no separate Tests page." User said "run panel with this" (no roster edits). Confirms: re-convening a focused subset for a follow-up decision works (skill note).
- Votes: A (Product, Competitive, Restraint) · refine→B (QA) · refine (UX/IA). **Director verdict = the UX/IA refine**, which reconciles the A-vs-B split.
- **Decision: keep Overview as the cross-metric DASHBOARD (reject splitting it = option C dead); RENAME "Test timing" → "Tests"** and make it the test-execution deep-dive (KPI tiles pass%/failures/suite-time/Δ → pass-fail trend w/ toggle → searchable **all-tests roster**: name·status·pass-rate·flaky-badge·p50, row → the existing per-test `test.html`). **No new tab** (stays 6: Overview·Coverage·Tests·Load tests·Flaky·Failure clusters).
- The decisive find (QA, grounded in templates): the per-test page **`test.html` already exists and is excellent** (status timeline + duration trend + first-failing blame + history) but is **ORPHANED** — reachable only by clicking a Flaky/Timing row, breadcrumb wrongly parented under "Performance", and there is **no test index / no search**. The rename gives it a real home + a searchable index, filling the only genuine gap.
- Competitive (cited): **no mature tool has a separate "Tests" landing distinct from its overview** — SonarQube/Codecov/Datadog all do summarize-**and-link** (overview summarizes, deep-dive owns the table). Allure's separate Dashboards+Test-results+Trends is the cautionary fragmentation. So Overview legitimately repeats coverage%/pass% as linking summaries — not duplication.
- Restraint (held the line, 8×+): reject B/C as tab-sprawl; the fix is labeling + linking + unify the 3 deep-dives; a 7th tab must retire one. The rename satisfies this (addition-free).
- Overlap fix = direction of links: Overview tiles/sections deep-link DOWN into each metric page ("how healthy, across metrics" → "why, within one metric"). Flaky + Failure clusters become slices cross-linked from Tests.
- Mockups updated (static, real app.css): all 4 + the renamed Tests deep-dive; sent for review. Open follow-up: the all-tests **roster + search** is a small new read-side feature (widen slowest-tests → all tests + filter), beyond pure layout — scope it into the Tests-page phase.

## 2026-06-30 — UI data-map cross-tab reconciliation (improve existing artifact / IA)
- Proposed: UX/IA (director), Frontend/design-system, Competitor analyst, QA, Restraint skeptic. User added: **Developer + Performance Engineer** ("data is also for them not just for testers") — for analytics/IA targets here, seat consumer-advocate roles for each audience, not just QA.
- Style: swarm → director-led, 1 round (strong convergence; cap was 2, didn't need round 2).
- User pre-steer at convene time: Flaky + Clusters are NOT separate tabs — sections of Tests; "nothing to drill → show inline." Director should make the IA the director (not visual designer) when the target is navigation structure, not visual polish.
- Resolved unanimously: **flag/branch = persisted scope dropdowns in subnav; module/label = the one breakdown-list idiom; kill the pill bar** (Load tests). Perf eng's framing (flag=scenario=scope, label=transaction=breakdown) settled the long-open flag-vs-module question.
- Real split surfaced for the user: **Restraint Skeptic wants Test timing MERGED into Tests** ("Tests sorted by duration") → 6 tabs→4; Perf eng kept them distinct. Director recommended keep-as-tab (epic #390 = unify four, not delete) but flagged as user decision T.
- Competitor nuance worth keeping: fold flaky/cluster *summaries* into Tests but DON'T delete the flaky state-management roster / cluster detail (Datadog flaky-mgmt, Allure Categories are first-class) — "inline entry point, full surface as drill."
- Recurring (3rd UI target): the "skeleton = slots that may be empty/absent" + first-run/empty-state discipline keeps coming up; and "blue name ⇔ real drill" (never fake-blue a dead-end) is now a standing repo rule.

## 2026-06-30 — all-tests roster slot: does a 1200-row table earn its place? (stress-test + reshape)
- Proposed: UX/IA (director), QA/Tester, Developer, Competitive analyst, Frontend/design-system, Restraint skeptic. User edits: **none** ("go"). Confirms (2nd analytics target running): seat per-audience consumer advocates (QA + Developer) on data/IA targets; the visual Designer is the right seat to DROP when the question is "what is this for," not "how does it look."
- Style: swarm → director-led, 1 round — exceptionally strong convergence (5 of 6 independently said "flat 1200-row default has no job; default to failures-first + collapse passing").
- UNANIMOUS: the flat full-list default is the JUnit-HTML-report anti-pattern; nobody scrolls past ~row 30. Competitor evidence: **no rival opens with a flat list** (Datadog → failed→flaky→slowest + server paging; Allure → collapsible suites tree + Categories; Codecov Test Analytics → top-N aggregate).
- The one real split (the user's call): **Restraint = CUT the table, keep only a typeahead→test-history** (failures→Clusters, flaky→Flaky section, slow→Timing tab, history→test.html all serve the rest); **everyone else = SHRINK** to a default "needs-attention" view + search + lazy "show all N". Director resolved **shrink**, because "what failed in THIS run" is genuinely under-served elsewhere on the Tests tab (Clusters only shows signature-grouped/recurring; a singleton non-recurring failure appears in neither folded section) — but adopted Restraint's anti-duplication guardrails wholesale (no quarantine action, no per-test sparkline, don't rebuild the Timing tab in rows).
- Recurring (now multiple targets): Restraint's "shrink, don't bolt on" + the matters-view default (failing/flaky/changed first, collapse the green mass) is the same pattern as the skeleton/empty-state discipline — promote to a standing rule soon.
- Data-feasibility caveat surfaced by the director (repo quirk): per-test **coverage cell** (Competitive's "row Datadog+Codecov each show half of") is NOT in the data model (coverage is per-run/per-file, not per-test) → P3-aspirational, likely infeasible; **retry/attempt count** depends on whether ingest parses Surefire rerun elements (verify before promising). NEW/STILL-FAILING/FIXED novelty + Δduration ARE cheap read-side (controller already loads prev run).

## 2026-07-01 — Review the 4 reconciled analytics pages (acceptance/data-correctness review of implemented artifacts)
- Proposed: UX/IA (director), QA/Tester, Developer, Performance Engineer, Data-Correctness/Metrics Skeptic, Competitive Analyst, Restraint Skeptic. User edits: **none** ("go as-is"). Confirms again: on data/consistency targets DROP the visual Designer and seat per-audience advocates (QA+Dev+Perf) + a **Data-Correctness skeptic**; IA directs when the question is structure/data, not visual polish.
- New role that paid off big: the **Data-Correctness/Metrics Skeptic** (traces every number to source). It found the sharpest bug (Coverage KPI tiles NOT re-scoped under `?module=` while the "Module: X" chip shows — tiles display project-wide numbers) and CLEARED false alarms (no empty-state sibling; delta directions/baselines/second-axis all correct). Seat it by default on any "are the numbers right" target here.
- Style: swarm → director-led, 1 round (7 independent reviews; strong convergence). No round 2 needed.
- **Convergent CONFIRMED bugs (multi-reviewer):** (1) Coverage tiles/trend ignore `?module=` scope [Data HIGH]; (2) Coverage headline vs Δ/trend read different run-series (latestCoverage any-flag vs trendRuns default-flag) → wrong pp baseline [QA+Data HIGH]; (3) Load "Recent perf runs" table renders OUTSIDE the `hasRun` guard → same double-render class just fixed [Restraint]; (4) Tests by-module Pass% denominator (incl. skipped) ≠ scoped KPI pass-rate (excl. skipped) → same module shows 80% in list, 100% scoped [Data]; (5) Timing "Δ total" breakdown column has no up/down color (slower looks like faster) [Perf]; (6) one-dot trend on first run — D6 "≥2 points or empty" enforced nowhere, first-run hint is dead code [IA+QA+Restraint]; (7) Timing+Load MISSING the empty-state-absent regression test that Tests+Coverage got [QA].
- **Depth (more/less) — expert calls:** LESS: Coverage instruction/method tiles are JaCoCo-noise (Codecov shows line+branch) [Competitive]; Load regression σ → tooltip; Tests 3-way failure taxonomy (clusters+recurring+flaky) → collapse to 2. MORE: Load per-transaction/LABEL breakdown is the empty primary slot + its defining benchmark view [Competitive HIGH, IA MED] — but Restraint VETOES as a tab (not in aggregate data model; needs a new across-runs query) → **DEFER, user call**. Last-hop drill-outs: `Project.repoUrl` used NOWHERE — wire it to coverage worst-file (→GitHub blob), every SHA (→commit), and the Load regression onset (add `onsetRunId` to PerfStepSignal → perf-run) [Dev HIGH + Perf MED]. p95 headline absent from its own latency chart (plots p50/p90/p99) [Perf+Data].
- **Spec drift (phase, not mistake):** Load flag pill-bar never killed + branch/flag scope dropdowns never added to subnav (D1/D2, tracked #405) [IA HIGH, Competitive MED]. Restraint: AnalyticsPage interface is duck-typed dead weight — don't grow it, don't churn it.
- Restraint wins again (9×+): the correct output is shrink/fix, not add-depth-to-prove-completeness. "The one thing: move Load's table inside hasRun." Director expanded that to the P0 data-correctness cluster (all cheap, in-charter) since the user chartered a correctness pass.
- Note (durable): the empty-state double-render (th:replace precedence 100 < th:if 300) recurred as a FAMILY — the guarded-fragment bug was fixed, but a sibling (detail table outside the guard) survived, and 2 pages lacked the regression test. When fixing a render-guard bug here, sweep ALL pages + lock each with a present-AND-absent assertion.

## 2026-07-02 — Landing page for 3 audiences: summary + data + design (marketing, new work type)
- Proposed: Product-Marketing Lead (director), Competitive Analyst, Developer-Buyer Advocate, Conversion Designer, Technical Copywriter, Anti-Hype Skeptic. **User added: QA Advocate + Performance Advocate** ("also landing page for qa developers and performance engineers, add accordingly"). Removed: none.
  - **Gate-edit signal (durable):** for a **multi-audience marketing page** here, seat **one persona-advocate per named audience** by default (Dev/QA/Perf), not a single "buyer" seat — the productive tension is *whose pain leads the hero*. This is a distinct roster from the app-UI target (design-trio+QA+Restraint); marketing target = Marketing-Lead + Competitive-Analyst + per-persona advocates + Designer + Copywriter + Anti-Hype Skeptic.
- Style: swarm → director-led, **1 round**, strong convergence. Central-tension vote **7 BLEND / 1 unified** → **unified hero + three co-equal persona lanes** (never "three doors").
- **Positioning canon (graduate — reuse verbatim):** the wedge is *"the only free, self-hosted tool that shows a coverage drop, a flaky test, and a p99 regression for the **same commit**"* — a sentence **no single competitor can print** (Codecov=no perf; Datadog/Trunk=SaaS+no coverage; Allure/ReportPortal=no coverage; SonarQube≠test report). Competitor device = a tight **6×6 matrix** (Codecov/Datadog/Trunk/Allure/SonarQube/UniTrack × Tests/Coverage/Perf-JMH/Flaky/Free-self-host); the two UniTrack-only columns are **Perf/JMH** and **Free self-host**.
- **Recurring (Anti-Hype Skeptic wins credibility scope, now on a marketing target too):** kill/soften **polyglot** (JVM-only proof), **AI-powered** (needs user's own Anthropic key → "optional AI triage, BYO key"), **GitHub App/Checks** (only commit-status+PR-comment ship — say exactly that), **enterprise/team/SSO** (0.x solo). **Flaky is table-stakes, not the headline** (rivals match/beat). Weaponize maturity ("0.x, self-hosted, read every line") instead of hiding it.
- **Durable landing-page insight (Dev Advocate):** a dashboard beauty-shot reads as *someone else's data*; the **failing quality-gate + PR-comment screenshot** reads as *this will guard MY PR* → hero pairs the unified board (comprehension) with the failing-gate + 5-line CI snippet (loop-closes-in-my-CI). Primary CTA = **Try the live demo** (cheapest, strongest proof asset), repeated in nav/hero/final.
- **The one real split (unresolved, user to overrule):** perf prominence — Perf+Analyst want a co-equal lane (cleanest differentiator, "runs unopposed"); Skeptic says perf is "real but niche," mostly a demo screenshot → bury under "More, labeled early." Director kept perf as a co-equal lane **with** the Skeptic's honesty guardrails (no perf headline, no JMH-depth overclaim). Recurring theme: **perf = strongest differentiator, narrowest audience.**
- Output: `presentation/landing-page-brief.md` (summary + landing data + section-by-section design). Ready to render as an HTML Artifact.
